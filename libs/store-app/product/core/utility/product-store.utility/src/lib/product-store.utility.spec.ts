import { productStoreUtility } from './product-store.utility';

describe('productStoreUtility', () => {
  it('should work', () => {
    expect(productStoreUtility()).toEqual('product-store.utility');
  });
});
