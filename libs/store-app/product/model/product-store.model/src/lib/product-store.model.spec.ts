import { productStoreModel } from './product-store.model';

describe('productStoreModel', () => {
  it('should work', () => {
    expect(productStoreModel()).toEqual('product-store.model');
  });
});
