import { productStoreMapper } from './product-store.mapper';

describe('productStoreMapper', () => {
  it('should work', () => {
    expect(productStoreMapper()).toEqual('product-store.mapper');
  });
});
